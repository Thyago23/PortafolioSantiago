{
  "version": 2,
  "buildCommand": "npm install --prefix client && cd client && npm run build",
  "outputDirectory": "client/dist",
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs18.x"
    }
  }
}
